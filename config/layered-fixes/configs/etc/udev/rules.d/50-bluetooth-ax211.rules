# Zephyrus GU605MY — Disable USB autosuspend for Intel AX211 Bluetooth
# Prevents mouse/keyboard disconnects during idle
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="8087", ATTR{idProduct}=="0033", ATTR{power/control}="on"
